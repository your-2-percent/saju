{
  "name": "saju",
  "private": true,
  "packageManager": "pnpm@10.6.5",
  "workspaces": [
    "packages/*",
    "apps/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:attw": "turbo run test:attw"
  },
  "devDependencies": {
    "turbo": "^2.4.4"
  }
}
